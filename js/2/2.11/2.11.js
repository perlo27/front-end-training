//Check the range between

if (age >= 14 && age <= 90);

//Check the range outside

if (age < 14 || age > 90);




//A question about "if"

if (-1 || 0) alert( 'first' );    //show
if (-1 && 0) alert( 'second' );     //no
if (null || -1 && 1) alert( 'third' ); //show